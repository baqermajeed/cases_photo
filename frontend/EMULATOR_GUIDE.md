# 📱 دليل التشغيل على Android Emulator

## ✅ الإعدادات الحالية

تم ضبط التطبيق للعمل على **Android Emulator** بالإعدادات التالية:

- **API URL**: `http://10.0.2.2:5030` ✅
- **Emulator**: Pixel 7 (Android 16) ✅
- **Backend**: يعمل على `localhost:5030` ✅

---

## 🚀 طريقة التشغيل السريعة

### الطريقة 1: باستخدام السكربت (الأسهل)

```powershell
cd C:\Users\ENG\Desktop\cases_photo\frontend
.\run_on_emulator.ps1
```

السكربت سيقوم بـ:
1. التحقق من Backend ✓
2. التحقق من الإيميوليتر وتشغيله إذا لزم ✓
3. تشغيل التطبيق ✓

---

### الطريقة 2: يدوياً (خطوة بخطوة)

#### 1. تأكد من Backend يعمل

```powershell
# اختبار
Invoke-WebRequest -Uri "http://localhost:5030/health" -UseBasicParsing

# إذا لم يعمل، شغّله:
cd C:\Users\ENG\Desktop\cases_photo\backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 5030 --reload
```

#### 2. شغّل الإيميوليتر

```powershell
flutter emulators --launch Pixel_7

# انتظر 20-30 ثانية حتى يفتح الإيميوليتر
```

#### 3. تأكد من الإيميوليتر شغال

```powershell
flutter devices
```

يجب أن ترى:
```
sdk gphone64 x86 64 (mobile) • emulator-5554 • android-x64 • Android 16
```

#### 4. شغّل التطبيق

```powershell
cd C:\Users\ENG\Desktop\cases_photo\frontend
flutter run -d emulator-5554
```

---

## 🎮 أوامر مفيدة أثناء التشغيل

بعد ما يشتغل التطبيق، يمكنك:

| المفتاح | الوظيفة |
|---------|---------|
| `r` | Hot Reload (إعادة تحميل سريعة) |
| `R` | Hot Restart (إعادة تشغيل كاملة) |
| `v` | فتح DevTools |
| `q` | إيقاف التطبيق |
| `h` | عرض المساعدة |

---

## 📋 الإيميوليترات المتاحة

عندك 8 إيميوليترات:

```powershell
flutter emulators
```

| ID | الجهاز | الحالة |
|----|--------|--------|
| `Pixel_7` | Pixel 7 | ✅ موصى به |
| `Pixel_8` | Pixel 8 | ✅ سريع |
| `Medium_Phone` | Medium Phone | ⚡ خفيف |
| `Small_Phone` | Small Phone | ⚡ أخف |

**لتشغيل إيميوليتر آخر:**
```powershell
flutter emulators --launch Pixel_8
```

---

## 🔧 حل المشاكل

### ❌ خطأ: "No devices available"

**الحل:**
```powershell
# شغّل إيميوليتر
flutter emulators --launch Pixel_7

# انتظر 30 ثانية
Start-Sleep -Seconds 30

# تحقق
flutter devices
```

---

### ❌ خطأ: "Failed to connect to backend"

**السبب:** عنوان API خاطئ

**تحقق:**
```dart
// في: frontend/lib/core/constants/api_constants.dart
static const String baseUrl = 'http://10.0.2.2:5030';  // يجب أن يكون هكذا
```

⚠️ **مهم جداً:**
- على **Emulator**: استخدم `10.0.2.2`
- على **جهاز حقيقي**: استخدم `192.168.0.104`

---

### ❌ خطأ: "Gradle build failed"

**الحل:**
```powershell
cd C:\Users\ENG\Desktop\cases_photo\frontend

# نظف المشروع
flutter clean

# احصل على الحزم
flutter pub get

# شغّل مرة ثانية
flutter run -d emulator-5554
```

---

### ❌ الإيميوليتر بطيء جداً

**الحلول:**

1. **استخدم إيميوليتر أخف:**
   ```powershell
   flutter emulators --launch Small_Phone
   ```

2. **قلل رزوليشن الإيميوليتر:**
   - افتح AVD Manager
   - Edit الإيميوليتر
   - غيّر Resolution لـ 720x1280

3. **زود RAM للإيميوليتر:**
   - AVD Manager → Edit
   - Advanced Settings
   - RAM: 2048 MB أو أكثر

---

## 🧪 اختبار الاتصال من الإيميوليتر

بعد ما يشتغل الإيميوليتر:

1. افتح **Chrome** في الإيميوليتر
2. اكتب في المتصفح:
   ```
   http://10.0.2.2:5030
   ```
3. يجب أن تشوف:
   ```json
   {"service": "FarahDent Backend", "version": "1.0.0"}
   ```

إذا شفته = ✅ الاتصال شغال!

---

## 📊 معلومات الإيميوليتر

### عنوان localhost الخاص

| من | إلى | العنوان |
|----|-----|---------|
| Emulator | PC localhost | `10.0.2.2` |
| Emulator | PC 127.0.0.1 | `10.0.2.2` |
| PC | Emulator | `localhost` أو `127.0.0.1` |

### منافذ الشبكة

| الخدمة | المنفذ | الوصول |
|--------|--------|--------|
| Backend API | 5030 | `http://10.0.2.2:5030` |
| Flutter DevTools | متغير | تلقائي |
| ADB | 5037 | تلقائي |

---

## 🎯 نصائح للتطوير

### 1. Hot Reload السريع
- اعمل تعديلات في الكود
- اضغط `r` في terminal
- التطبيق يتحدث فوراً بدون إعادة تشغيل!

### 2. Console Logs
شوف الـ logs في Terminal:
```dart
print('رسالة للتصحيح');
debugPrint('معلومات مفصلة');
```

### 3. Flutter DevTools
```powershell
# افتح DevTools
flutter pub global run devtools
```

ثم اضغط `v` في Terminal

---

## 📱 معلومات تسجيل الدخول

بعد ما يفتح التطبيق:

- **Username**: `admin`
- **Password**: `admin123`

---

## 🎊 كل شيء جاهز!

الإعدادات الحالية:
- ✅ Backend شغال على `localhost:5030`
- ✅ Frontend مضبوط على `http://10.0.2.2:5030`
- ✅ Emulator: Pixel 7 متاح
- ✅ Admin user موجود

**شغّل الآن:**
```powershell
cd frontend
.\run_on_emulator.ps1
```

أو:
```powershell
flutter run -d emulator-5554
```

---

**تم التحديث:** 11 نوفمبر 2025
**الحالة:** ✅ جاهز للتشغيل على Emulator
