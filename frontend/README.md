# FarahDent - تطبيق إدارة حالات المرضى 🦷

تطبيق Flutter متكامل لإدارة بيانات مرضى عيادة الأسنان مع تتبع مراحل العلاج والصور.

## ✨ المميزات

- 🔐 تسجيل دخول آمن للمصورين
- 👥 إدارة بيانات المرضى (إضافة، عرض، بحث)
- 📸 رفع وإدارة صور المراحل العلاجية (22 خطوة)
- 📊 تتبع تقدم العلاج لكل مريض
- 🎨 تصميم طبي أنيق بالعربية (RTL)
- 🔄 مزامنة مباشرة مع الخادم

## 📋 المتطلبات

- Flutter SDK 3.9.2 أو أعلى
- Dart 3.x
- محاكي Android/iOS أو جهاز حقيقي
- Backend server يعمل على المنفذ 5030

## 🚀 التثبيت والتشغيل

### 1. تثبيت الحزم

```bash
cd frontend
flutter pub get
```

### 2. إعداد البيئة

افتح الملف `lib/core/constants/api_constants.dart` وعدّل عنوان الخادم:

```dart
static const String baseUrl = 'http://YOUR_IP:5030';
```

💡 **ملاحظة مهمة:** 
- على الإيميوليتر Android: استخدم `http://10.0.2.2:5030`
- على الجهاز الحقيقي: استخدم IP جهاز الكمبيوتر (مثل `http://192.168.1.100:5030`)
- تأكد أن الجهاز والكمبيوتر على نفس الشبكة

### 3. تشغيل التطبيق

```bash
flutter run
```

أو للتشغيل بوضع Debug مع Hot Reload:

```bash
flutter run --debug
```

### 4. بناء التطبيق للإنتاج

#### Android (APK):
```bash
flutter build apk --release
```
الملف الناتج في: `build/app/outputs/flutter-apk/app-release.apk`

#### iOS:
```bash
flutter build ios --release
```

## 📱 الاستخدام

### 1. تسجيل الدخول
- استخدم بيانات المصور (username & password)
- البيانات الافتراضية: `admin` / `admin123`

### 2. إدارة المرضى
- **عرض القائمة**: تظهر جميع المرضى مع نسبة التقدم
- **البحث**: يمكن البحث بالاسم أو رقم الهاتف
- **إضافة مريض**: اضغط زر ➕ وأدخل البيانات

### 3. تفاصيل المريض
- **رفع الصور**: اضغط "رفع صور" لأي خطوة واختر صور متعددة
- **عرض الصور**: اضغط على أي صورة لعرضها بحجم كامل
- **حذف صور**: اضغط ❌ على الصورة
- **تمييز منجز**: اضغط "تم" لتحديد الخطوة كمنجزة

## 🗂 هيكل المشروع

```
lib/
├── core/
│   ├── constants/       # ثوابت API
│   └── theme/          # Theme وألوان التطبيق
├── models/             # نماذج البيانات (User, Patient, Step...)
├── screens/            # شاشات التطبيق
│   ├── login_screen.dart
│   ├── patients_list_screen.dart
│   ├── add_patient_screen.dart
│   ├── patient_detail_screen.dart
│   └── profile_screen.dart
├── services/           # خدمات API
│   ├── auth_service.dart
│   └── patient_service.dart
└── main.dart          # نقطة الدخول
```

## 🎨 التصميم

التطبيق يستخدم:
- **الخط**: Cairo (Google Fonts)
- **الألوان الأساسية**:
  - أزرق رئيسي: `#0284C7`
  - أزرق فاتح: `#E0F2FE`
  - خلفية: `#F8FAFC`
- **المكونات**: Material 3 مع تخصيص كامل
- **الاتجاه**: من اليمين لليسار (RTL)

## 🔧 استكشاف الأخطاء

### مشكلة الاتصال بالخادم
```
تعذر الاتصال بالخادم
```
**الحل:**
1. تأكد أن Backend يعمل على `http://localhost:5030`
2. استخدم IP الصحيح حسب جهازك
3. تأكد من السماح للتطبيق بالوصول للشبكة

### مشكلة رفع الصور
```
فشل في رفع الصور
```
**الحل:**
1. تأكد من صلاحيات الكاميرا والتخزين
2. تحقق من حجم الصور (أقل من 10 MB)
3. تحقق أن نوع الصورة مدعوم (JPEG/PNG/WebP)

### خطأ في التوكن
```
Invalid token / User not found
```
**الحل:**
1. سجل خروج ثم أعد تسجيل الدخول
2. امسح بيانات التطبيق وأعد المحاولة

## 📦 الحزم المستخدمة

| الحزمة | الاستخدام |
|--------|-----------|
| `http` | طلبات HTTP |
| `flutter_secure_storage` | تخزين التوكن آمن |
| `image_picker` | اختيار الصور |
| `cached_network_image` | عرض الصور مع Cache |
| `photo_view` | عرض الصور بحجم كامل |
| `google_fonts` | خط Cairo |
| `intl` | تنسيق التواريخ |

## 📝 ملاحظات مهمة

1. **الأمان**: لا تشارك JWT Token أو بيانات الدخول
2. **الصور**: يتم رفعها لـ Cloudflare R2 حسب إعدادات الخادم
3. **الأداء**: التطبيق يستخدم Caching للصور لتحسين الأداء
4. **الشبكة**: يتطلب اتصال إنترنت دائم

## 🤝 المساهمة

هذا المشروع مخصص لعيادة FarahDent. للتعديلات أو المساعدة، تواصل مع فريق التطوير.

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 FarahDent Clinic

---

**تم التطوير بـ ❤️ باستخدام Flutter**
