# 🚀 دليل التشغيل السريع - FarahDent

## خطوات التشغيل (5 دقائق فقط!)

### 1️⃣ تأكد من تشغيل الـ Backend

```powershell
# في مجلد backend
Set-Location 'C:\Users\ENG\Desktop\cases_photo'
Start-Job -Name "farahdent-api-5030" -ScriptBlock {
  Set-Location -Path 'C:\Users\ENG\Desktop\cases_photo'
  python -m uvicorn app.main:app --host 0.0.0.0 --port 5030
}
```

تأكد أن الخادم يعمل على: **http://localhost:5030**

---

### 2️⃣ إعداد عنوان API

**افتح:** `frontend/lib/core/constants/api_constants.dart`

**عدّل السطر 3:**

```dart
// للتشغيل على الإيميوليتر
static const String baseUrl = 'http://10.0.2.2:5030';

// أو للتشغيل على جهاز حقيقي (ضع IP جهازك)
// static const String baseUrl = 'http://192.168.1.100:5030';
```

💡 **لمعرفة IP جهازك:**
```powershell
ipconfig
# ابحث عن: IPv4 Address في Wi-Fi أو Ethernet
```

---

### 3️⃣ تشغيل التطبيق

```powershell
cd frontend
flutter run
```

**أو اختر الجهاز يدوياً:**
```powershell
flutter devices              # اعرض الأجهزة المتاحة
flutter run -d <device-id>  # شغّل على جهاز محدد
```

---

### 4️⃣ تسجيل الدخول

**البيانات الافتراضية:**
- Username: `admin`
- Password: `admin123`

---

## ✅ التحقق من التشغيل

1. **الخادم يعمل؟**
   ```powershell
   Invoke-WebRequest -Uri "http://localhost:5030/health" -UseBasicParsing
   ```
   يجب أن ترى: `{"ok":true}`

2. **التطبيق متصل؟**
   - افتح التطبيق
   - سجل دخول
   - إذا دخلت لصفحة المرضى = ✅ نجح!

---

## 🔧 حل المشاكل السريع

### ❌ خطأ: "تعذر الاتصال بالخادم"

**السبب:** عنوان API خاطئ

**الحل:**
1. تأكد من IP الصحيح في `api_constants.dart`
2. على الإيميوليتر: استخدم `10.0.2.2`
3. على الجهاز: استخدم IP الكمبيوتر

---

### ❌ خطأ: "Invalid token"

**الحل:**
- اضغط على أيقونة الحساب (أعلى يمين)
- اضغط "تسجيل الخروج"
- سجل دخول مرة أخرى

---

### ❌ الصور لا تُرفع

**تحقق من:**
1. صلاحيات الكاميرا والتخزين في الجهاز
2. حجم الصورة أقل من 10 MB
3. نوع الصورة: JPEG/PNG/WebP

---

## 📱 الصفحات الرئيسية

| الصفحة | الوصف |
|--------|-------|
| **تسجيل الدخول** | أول صفحة - أدخل username & password |
| **قائمة المرضى** | عرض وبحث عن المرضى + نسبة التقدم |
| **إضافة مريض** | نموذج بسيط لإضافة مريض جديد |
| **تفاصيل المريض** | 22 خطوة مقسمة على 3 مراحل |
| **الحساب** | بيانات المصور + تسجيل خروج |

---

## 🎯 نصائح الاستخدام

✅ **للرفع السريع:** اختر عدة صور دفعة واحدة (Multi-select)
✅ **للعرض الكامل:** اضغط على أي صورة لتكبيرها
✅ **للبحث:** اكتب الاسم أو رقم الهاتف في حقل البحث
✅ **للتحديث:** اسحب القائمة للأسفل (Pull to refresh)

---

## 📞 المساعدة

إذا واجهت مشكلة:
1. تحقق من سجلات الـ Backend: `Receive-Job -Name "farahdent-api-5030" -Keep`
2. تحقق من سجلات Flutter في الطرفية
3. أعد تشغيل الخادم والتطبيق

---

**بالتوفيق! 🎉**
